# 素材库工作流与数据设计

## 当前目标

素材是可以长期复用的基础资产，内容只是一次选图结果。两者不能混在同一个临时草稿流程中。

- **素材**保存原图、缩略图、主图/次图默认图位、多个标签和复用记录。
- **内容包**保存标题，以及所选素材的顺序。第 1 张作为主图，其余作为次图。
- 同一素材可以出现在多篇内容中，不复制原件；修改内容顺序也不会修改素材本身。

## 操作闭环

`上传成品图 → 逐图设置主/次图并打标签 → 按标签筛选 → 创建内容并选图 → 调整顺序 → 下载 ZIP 或生成文件夹 → 写入复用记录`

上传完成后自动选中新图片并进入打标，减少素材落入“上传了但没有整理”的状态。创建内容时选图、排序和导出在同一页面完成，避免在素材库和拼装页之间反复跳转。

## 参考项目带来的设计取舍

- [PhotoPrism 的批量编辑](https://github.com/photoprism/photoprism/blob/develop/internal/photoprism/batch/README.md)把多选、批量元数据和标签处理放在素材浏览主流程中，因此本系统也让上传后的新素材直接进入批量整理。
- [TagSpaces](https://github.com/tagspaces/tagspaces)以标签和搜索组织本地文件，因此素材的主题、空间和风格采用可叠加标签，而不是把图片复制到多个实体文件夹。
- [ResourceSpace](https://github.com/resourcespace/resourcespace)使用 collection 汇集资源并支持归档下载，因此本系统把“一篇内容”建模为一组素材的有序集合，再导出 ZIP 或本地文件夹。
- [Immich 的手动排序讨论](https://github.com/immich-app/immich/discussions/1689)指出顺序属于素材与相册之间的关系，而不是素材本体。本系统同样把顺序和主/次位置保存在内容包中。

## 后续计费扩展

当前版本不显示尚未确定规则的金额，但数据模型预留以下独立能力：

- 计价规则：计费单位、主图/次图或内容类型、单价、币种、生效时间。
- 价格快照：内容结算时保存当时采用的单价，避免未来调价改写历史收入。
- 收入字段：预计收入、实际收入、结算状态和结算日期。
- 复用计费：明确同一素材被再次使用时，是否再次计价。

进入计费开发前需要确认四项规则：按图片还是按整套内容计费；主图和次图是否同价；复用是否重复计费；记录毛收入还是扣费后的实际收入。
